{% block body %}
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">

<div class="container" style="background-color: #f2f2f2;border: 2px solid black;margin-top: 2%">

    <div name="tete" style="text-align: center; border: 2px solid black; background-color: #c9d9f2; margin-top: 1%">
    <h4>
        <a href="{{path('identification')}}" style="color:black">Déconnexion</a>
        <a> . </a>
        <a href="{{path('app_for_supp')}}" style="color:black">retour à la liste des formations</a>
        <a> . </a>
        <a href="{{path('app_inscrit')}}" style="color:black">valider les inscriptions</a></br></br>
        <a href="{{path('aff_formation')}}" style="color:black">Voir les employes</a></br></br>
    </h4>
    {% if (message) %}
        {{ message }}
    {% endif %}

    {% for inscription in ensInscription %}
    {{ inscription.id }}
    <br>
    {% endfor %}

    </div>
    <br>
</div>

{% endblock %}
